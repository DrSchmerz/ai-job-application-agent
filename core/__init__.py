"""Reusable core for the application engine: matching, LLM providers, logging.

These modules are UI-agnostic and dependency-light so they can back the current
Streamlit app, the MCP server, the CLI, and a future multi-user web product alike.
"""
